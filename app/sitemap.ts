import type {MetadataRoute} from "next";import {services,guides} from "@/lib/content";
export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://proper-move-in-cleaning.vercel.app";const fixed=["","/estimate","/service","/cleaning","/guide","/privacy","/terms"];const service=Object.keys(services).map(x=>`/service/${x}`);const guide=Object.keys(guides).map(x=>`/guide/${x}`);const regions=["/cleaning/seoul/gangnam","/cleaning/seoul/songpa","/cleaning/busan/haeundae","/cleaning/daegu/suseong","/cleaning/gyeonggi/suwon"];return [...fixed,...service,...guide,...regions].map(path=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:path?"monthly":"weekly",priority:path?0.7:1}))}
