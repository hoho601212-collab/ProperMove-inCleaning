import {notFound} from "next/navigation";import {guides} from "@/lib/content";import {INQUIRY_URL} from "@/lib/inquiry";
const checks=["작업 공간이 비어 있는지 확인","수도와 전기 사용 가능 여부 확인","공동현관 출입방법과 주차 확인","엘리베이터 사용·관리사무소 예약 확인","스티커·보호필름·폐기물 사전 공유","기본 범위와 별도 작업을 견적서에 표시"];
export function generateStaticParams(){return Object.keys(guides).map(slug=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const title=guides[slug as keyof typeof guides];return title?{title,description:`${title}에서 놓치기 쉬운 범위와 현장 조건을 확인하세요.`}:{} }
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const title=guides[slug as keyof typeof guides];if(!title)notFound();return <><section className="pageHero"><div className="shell"><span className="eyebrow">PRACTICAL GUIDE</span><h1>{title}</h1><p>현장과 업체 조건에 따라 달라질 수 있으므로 계약 전 포함 범위를 확인하세요.</p></div></section><section className="section shell"><div className="contentGrid">{checks.map((c,i)=><article className="contentCard" key={c}><span className="eyebrow">CHECK {i+1}</span><h2>{c}</h2><p>구두 안내에만 의존하지 말고 견적서나 메시지로 조건을 남겨 두는 편이 좋습니다.</p></article>)}</div><div className="areaBox" style={{marginTop:40}}><div><h2>확인한 조건으로 견적문의</h2><p>가격, 인원, 작업시간, 포함 범위와 추가요금 기준을 같은 기준으로 문의하세요.</p></div>{/* 기존 내부 견적 링크 임시 보존: /estimate */}<a className="button" href={INQUIRY_URL}>견적문의 →</a></div></section></>}
